<div>
    {{#with main}}
    <main>
        <section>
            {{> title data="My Educations" big=istrue}}
            <ul class="ps-5">
                {{#each educations}}
                <li>
                    {{name}}
                    {{#unless isEnd }} (ще в процесі) {{/unless}}
                </li>
                {{/each}}
            </ul>
        </section>

        <section>
            {{> title data="My Certificates"}}
            <ul class="ps-5">
                {{#each certificates}}
                <li>
                    {{name}} / ({{id}})
                </li>
                {{/each}}
            </ul>
        </section>
    </main>
    {{/with}}
</div>